import React from 'react';
import PropTypes from 'prop-types';

const Input = ({ }) => {
  return (
    <div>Input...</div>
  );
};

Input.propTypes = {};

Input.defaultProps = {};

export default Input;
